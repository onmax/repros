diff --git a/dist/index.mjs b/dist/index.mjs
index a58eb0d167f4887ad8b7e363d3a5d290dec0560e..4a8841224f5bd5b3d026f5c989cea85cfbb80be1 100644
--- a/dist/index.mjs
+++ b/dist/index.mjs
@@ -5063,6 +5063,12 @@ const nightlies = {
 };
 let warnedAboutCompatDate = false;
 async function initNuxt(nuxt) {
+  // Compute deterministic buildId for production builds (fix for #33999)
+  if (!nuxt.options.dev && !nuxt.options.test && !nuxt.options._prepare) {
+    const { hash } = await getVueHash(nuxt);
+    nuxt.options.buildId = hash;
+    nuxt.options.runtimeConfig.app.buildId = hash;
+  }
   const layerDirs = getLayerDirectories(nuxt);
   for (const config of nuxt.options._layers.map((layer) => layer.config).reverse()) {
     if (config.hooks) {
