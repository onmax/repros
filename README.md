# repro/vueuse-storage-redundant-serialization

This branch contains one reproduction and its controls. Follow the README inside each fixture directory.

- `vueuse-storage-redundant-serialization`
- `vueuse-storage-redundant-serialization-fix`
